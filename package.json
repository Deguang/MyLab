{
  "name": "test1",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "precommit-msg": "echo 'Pre-commit checks...' && exit 0",
    "lint": "eslint ./ --cache --ignore-pattern .gitignore",
    "check": "node check.js"
  },
  "pre-commit": [
    "precommit-msg",
    "check"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^3.0.1",
    "pre-commit": "1.1.3"
  },
  "dependencies": {
    "eslint": "^3.0.0",
    "pre-commit": "^1.1.3"
  }
}
